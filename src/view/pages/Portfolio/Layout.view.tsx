import React from "react";

import classes from "./portfolio.module.scss";

type Props = {};

const LayoutView = (props: Props) => {
  return <h1 className={classes["titl"]}>Our Works</h1>;
};

export default LayoutView;

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "bobbyhadz.com",
            "images.pexels.com"
        ]
    }
}

module.exports = nextConfig
